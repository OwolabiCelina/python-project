import random


def shuffle_num(number):
    num = number[0]
    num1 = sum[0]

    for i in range(num):
        for a in range(num1):
            if i < num & a > num1:
                num = i

    return num


random.shuffle
num = [2, 4, 6, 10, 100, 3, 5, 9]
print(shuffle_num(num))
